# تقرير فحص واختبار مشروع FluxWAN

**المشروع:** [ahmedha43/FluxWAN](https://github.com/ahmedha43/FluxWAN)  
**الفرع المفحوص:** `main`  
**مسار النسخة المحلية:** `/home/ubuntu/FluxWAN`  
**نطاق الفحص:** تنزيل المشروع، تثبيت أدوات البناء، تجميع البرنامج، تشغيل الاختبارات المحلية، وفحص صياغة السكربتات.

## الخلاصة التنفيذية

تم تنزيل المشروع بنجاح، ونجح هدف `make all` في بناء البرنامج الرئيسي `fluxwan` وبرنامج المختبر `fluxwan_lab`. كما نجحت اختبارات التحقق من إعدادات WAN، واختبارات مدير PPPoE، واختبارات المختبر المحاكية لموازنة الأحمال والجلسات الثابتة والتحويل الديناميكي وDHCP.

النتيجة ليست اعتمادًا ميدانيًا للنظام؛ فالاختبارات الحالية محلية ومحاكية بدرجة كبيرة، ولم تُشغّل اختبارات الشبكة الحقيقية أو اختبار XDP داخل النواة بسبب غياب Docker وQEMU وبيئة واجهات WAN متعددة في بيئة الفحص.

## النتائج

| الفحص | النتيجة | الملاحظات |
|---|---:|---|
| استنساخ المستودع | ناجح | النسخة المحلية مطابقة للفرع `main` عند بدء الفحص |
| `make all` | ناجح | تم إنتاج `fluxwan` و`fluxwan_lab` |
| `test_wan_validator` | ناجح | 4 من 4 حالات، بما فيها حالات الرفض المتوقعة |
| `test_pppoe_manager` | ناجح | 3 من 3 مجموعات اختبار |
| `fluxwan_lab` | ناجح | 4 من 4 اختبارات محاكاة |
| `test_live_nat46_translation.c` | ناجح بعد إضافة `-D_DEFAULT_SOURCE` | الاختبار يمر بعد استخدام أعلام GNU المناسبة |
| `xdp_packet_test.c` | ناجح بعد إضافة `-D_DEFAULT_SOURCE` | الاختبار يمر بعد استخدام أعلام GNU المناسبة |
| فحص Bash `bash -n` | ناجح | لم تظهر أخطاء صياغة في السكربتات المفحوصة |
| فحص Python `compileall` | ناجح | لم تظهر أخطاء صياغة Python |
| اختبار المختبر الحقيقي | لم يُشغّل | يتطلب صلاحيات root وبيئة شبكة افتراضية كاملة |

## تفاصيل الاختبارات الناجحة

اختبار التحقق من WAN مرّر الحالات التالية: قبول ثلاث جلسات PPPoE على منفذ واحد، ورفض عميلَي DHCP على المنفذ نفسه، ورفض خلط DHCP مع Static على المنفذ نفسه، ورفض استخدام منفذ LAN كمنفذ WAN.

اختبار PPPoE مرّر تهيئة وتدمير السياق، وفحص حالات المدير، واختبار التهيئة متعددة الجلسات. كما مرّر `fluxwan_lab` اختبارات توزيع 100,000 تدفق، وثبات الجلسات، والفشل والاستعادة الديناميكيين، وتخصيص DHCP المحاكى.

اختبارا الحزم المستقلان مرّرا بناء الحزمة، والتحقق من checksum، ومحاكاة SNAT، وثبات الجلسة. لكن نجاحهما لا يثبت أن برنامج eBPF محمّل فعليًا على واجهة شبكة أو أن مسار الحزمة يعمل داخل kernel.

## المشكلات والمخاطر المكتشفة

### 1. فشل ترجمة eBPF يُخفى داخل Makefile

أثناء `make all` حاول المشروع ترجمة `bpf/xdp_router.bpf.c` باستخدام clang بهدف BPF، لكن ظهرت الرسالة:

> `/usr/include/stdint.h:26:10: fatal error: 'bits/libc-header-start.h' file not found`

ومع ذلك يستمر البناء لأن أمر الترجمة ينتهي بـ `|| echo "BPF compilation skipped..."`. لذلك يعرض `make all` نجاحًا حتى عندما لا يتم إنتاج أو تحديث كائن BPF صالح. هذه مشكلة عالية الأولوية؛ إذ قد ينتج عنها برنامج مستخدم ناجح البناء مع datapath غير موجود أو قديم.

**التوصية:** اجعل فشل BPF يفشل هدف `bpf` في البناء الإنتاجي، أو افصل هدفًا صريحًا مثل `make host-tests` عن `make appliance`. كما ينبغي استخدام toolchain BPF مهيأ بوضوح، مع تضمينات kernel/libc المناسبة، والتحقق من وجود `bpf/xdp_router.bpf.o` وحداثته.

### 2. اختبارات الحزم لا تُبنى بالخيارات الافتراضية المعلنة

عند تجميع الاختبارات باستخدام `-std=c11` فقط ظهرت أنواع غير مكتملة مثل `struct ether_header` و`struct tcphdr` وثوابت `ETHERTYPE_IP`. نجح البناء بعد إضافة `-D_DEFAULT_SOURCE`. هذا يعني أن تعليمات البناء أو Makefile لا يقدمان مسارًا موحدًا لبناء هذه الاختبارات.

**التوصية:** إضافة `-D_DEFAULT_SOURCE` إلى أعلام اختبارات Linux أو تعريف هدف Make مستقل للاختبارات، وإضافة الاختبارين إلى نظام البناء بدل تجميعهما يدويًا.

### 3. الاختبارات الحالية لا تتحقق من XDP الحقيقي

الاختباران `test_live_nat46_translation.c` و`xdp_packet_test.c` يطبقان أجزاء من المحاكاة داخل برنامج الاختبار، ويطبعان PASS، لكنهما لا يثبتان تحميل برنامج eBPF الفعلي وتشغيله عبر `bpf_prog_test_run` في المسار الذي ينفذه المشروع حاليًا. لذلك يجب اعتبارهما اختبارات packet-model/simulation إلى أن يُربطا بكائن BPF فعلي وبـ libbpf وخرائطه.

### 4. تحذيرات سلامة وجودة في C

ظهر عدد كبير من التحذيرات، أبرزها تجاهل القيمة المرجعة من `system()`، وتحذيرات `strncpy` لاحتمال القطع، وتحذير `snprintf` لاحتمال اقتطاع النص في `src/net_apply.c`. لا تمنع هذه التحذيرات البناء، لكنها تستحق المعالجة لأن المشروع يتعامل مع أسماء واجهات وأوامر شبكة وتهيئة تشغيلية.

**التوصية:** فحص أطوال المدخلات قبل النسخ، استخدام دوال نسخ مع تحقق صريح من النهاية، فحص نتائج `system()` أو استبدالها باستدعاءات أكثر أمانًا، وزيادة أحجام المخازن أو استخدام `snprintf` مع اختبار قيمة الإرجاع.

### 5. اختلاف مسارات البناء

يوجد `Makefile` و`CMakeLists.txt`، لكنهما لا يقدمان نفس التغطية. فـ `Makefile` يضم `pppoe_manager.c` و`dns64_daemon.c` في البرنامج الرئيسي، بينما قائمة CMake لا تبدو متطابقة مع ذلك. هذا قد يؤدي إلى نجاح بناء مختلف عن المنتج الفعلي بحسب الأداة المستخدمة.

**التوصية:** اختيار نظام بناء أساسي واحد أو ضمان تطابق المصادر والأهداف والاختبارات بين النظامين، مع اختبار البناءين في CI.

## القيود

لم يتم تشغيل Docker لأن Docker غير متاح في بيئة الفحص. كما لم تُشغّل اختبارات الشبكة الحقيقية التي تغيّر إعدادات الواجهات أو تتطلب root، ولم يُختبر الإقلاع من ISO أو أجهزة RB5009/RB750Gr3. هذه الاختبارات تحتاج بيئة افتراضية أو عتادًا حقيقيًا، ولا ينبغي تشغيلها على جهاز إنتاجي دون عزل.

## التوصيات ذات الأولوية

| الأولوية | الإجراء |
|---|---|
| عالية | إيقاف إخفاء فشل ترجمة BPF والتحقق من artifact الناتج |
| عالية | إنشاء CI يبني البرنامج، BPF، وجميع الاختبارات على Linux |
| عالية | ربط اختبار XDP بكائن BPF فعلي بدل الاكتفاء بالمحاكاة |
| متوسطة | توحيد أعلام البناء وإضافة `-D_DEFAULT_SOURCE` لمسار الاختبارات |
| متوسطة | معالجة تحذيرات `system`, `strncpy`, و`snprintf` |
| متوسطة | توحيد Makefile وCMakeLists واختبار التكافؤ بينهما |

## ملفات وسجلات الفحص

النسخة المحلية موجودة في `/home/ubuntu/FluxWAN`. السجلات المؤقتة محفوظة في `/tmp/fluxwan-build.log`، كما توجد مخرجات الجلسات الكاملة تحت `/home/ubuntu/terminal_full_output/`.

## المراجع

[1]: https://github.com/ahmedha43/FluxWAN "FluxWAN GitHub repository"
[2]: https://github.com/ahmedha43/FluxWAN/blob/main/README.md "FluxWAN README and build instructions"
